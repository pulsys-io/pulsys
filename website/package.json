{
  "name": "pulsys-website",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "predev": "node scripts/sync-docs.mjs && node scripts/sync-benchmarks.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/sync-docs.mjs && node scripts/sync-benchmarks.mjs",
    "sync": "node scripts/sync-docs.mjs && node scripts/sync-benchmarks.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "screenshot": "npm run build && playwright test",
    "screenshot:update": "npm run build && playwright test --update-snapshots"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.40.0",
    "asciinema-player": "^3.15.1",
    "astro": "^7.0.8",
    "chart.js": "^4.5.1",
    "sharp": "^0.35.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  },
  "overrides": {
    "esbuild": "0.28.1",
    "astro": {
      "esbuild": "0.28.1"
    },
    "vite": {
      "esbuild": "0.28.1"
    }
  }
}
